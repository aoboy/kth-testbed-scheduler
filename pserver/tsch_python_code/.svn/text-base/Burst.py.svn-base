#!/usr/bin/env python
# -*- coding: utf-8 -*-

import os,sys,  getopt, getpass
import os.path, time

import subprocess

class Burst:
    def __init__(self):
        self.first_burst      = int(0)
        self.first_burst_init = int(0)
        self.first_burst_length = int(0)

        self.second_burst      = int(0)
        self.second_burst_init = int(0)
        self.second_burst_length = int(0)
        
    def xpto(self):
        pass